---
layout: vscode.njk
title: "Dev"
---
<h1 class="vscode-title">Dev Feed</h1>
<p class="vscode-subtitle">Notes, builds, and experiments.</p>

{% set devTags = collections.devByTag %}
{% if devTags.length > 0 %}
  {% for tagGroup in devTags %}
  <div class="post-group">
    <h2 class="post-group-title">{{ tagGroup.tag | capitalize }}</h2>
    <ul class="post-list">
      {% for post in tagGroup.posts %}
      <li>
        <a href="{{ post.url | url }}">{{ post.data.title }}</a>
        <span class="post-meta">{{ post.date | readableDate }}</span>
        {% if post.data.tags %}
        <span class="post-tags">
          {% for tag in post.data.tags %}
          {% if tag !== "dev" %}
          <span class="tag">{{ tag }}</span>
          {% endif %}
          {% endfor %}
        </span>
        {% endif %}
      </li>
      {% endfor %}
    </ul>
  </div>
  {% endfor %}
{% else %}
  <p class="no-posts">No posts yet. Start creating!</p>
{% endif %}
