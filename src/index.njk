---
layout: base.njk
title: "Home"
---
<section class="window-grid">
  <!-- Dev Section Preview (VS Code style) -->
  <div class="app-window window-card window-vscode" style="--accent: #4fc3f7;">
    <header class="window-bar">
      <div class="window-dots">
        <span class="dot dot-red"></span>
        <span class="dot dot-yellow"></span>
        <span class="dot dot-green"></span>
      </div>
      <div class="window-title">Dev</div>
      <div class="window-meta">VS Code</div>
    </header>
    <div class="window-body" style="padding: 0;">
      <div class="vscode-shell" style="font-size: 0.85rem;">
        <aside class="vscode-sidebar" style="padding: 12px;">
          <div class="sidebar-title">Folders</div>
          <ul class="sidebar-list">
            {% set tagCount = 0 %}
            {% for tagGroup in collections.devByTag %}
              {% if tagCount < 3 %}
                <li>📁 {{ tagGroup.tag | capitalize }}</li>
                {% set tagCount = tagCount + 1 %}
              {% endif %}
            {% endfor %}
          </ul>
        </aside>
        <div class="vscode-editor" style="padding: 12px;">
          <div style="margin-bottom: 12px; font-family: 'Space Grotesk', sans-serif;">
            <strong>Recent Posts</strong>
          </div>
          {% set postCount = 0 %}
          {% for post in collections.dev %}
            {% if postCount < 3 %}
              <div style="margin-bottom: 8px;">
                <a href="{{ post.url | url }}" style="color: var(--accent); text-decoration: none; font-size: 0.85rem;">{{ post.data.title }}</a>
              </div>
              {% set postCount = postCount + 1 %}
            {% endif %}
          {% endfor %}
          <div style="margin-top: 12px;">
            <a class="window-link" href="{{ '/dev/' | url }}" style="font-size: 0.8rem; padding: 6px 10px;">View All →</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- About Section Preview (Wikipedia style) -->
  <div class="app-window window-card window-wikipedia" style="--accent: #bda27e; background: #f8f9fa;">
    <header class="window-bar">
      <div class="window-dots">
        <span class="dot dot-red"></span>
        <span class="dot dot-yellow"></span>
        <span class="dot dot-green"></span>
      </div>
      <div class="window-title">About</div>
      <div class="window-meta">Wikipedia</div>
    </header>
    <div class="window-body" style="padding: 0; background: #fff;">
      <div class="wiki-shell" style="font-size: 0.85rem;">
        <aside class="wiki-sidebar" style="padding: 12px;">
          <div class="wiki-logo" style="font-size: 1.5rem; margin-bottom: 12px;">W</div>
          <div class="wiki-toc-title" style="font-size: 0.8rem;">Contents</div>
        </aside>
        <article class="wiki-article" style="padding: 12px 16px;">
          <h1 style="font-size: 1.2rem; margin: 0 0 4px 0; border-bottom: 1px solid #a2a9b1; padding-bottom: 4px;">Stan Brouwer</h1>
          <p class="wiki-subtitle" style="font-size: 0.75rem; margin: 0 0 12px 0;">From Wikipedia, the free encyclopedia</p>
          <p style="font-size: 0.8rem; margin: 0 0 12px 0; line-height: 1.4;">Multi-disciplinary builder and designer focused on creating thoughtful digital experiences.</p>
          <div style="margin-top: 12px;">
            <a class="window-link" href="{{ '/about/' | url }}" style="font-size: 0.8rem; padding: 6px 10px; background: #8b6f47;">Read More →</a>
          </div>
        </article>
      </div>
    </div>
  </div>

  <!-- Links Section Preview (Hacker News style) -->
  <div class="app-window window-card window-hn" style="--accent: #ff7e3d; background: #ffefe6;">
    <header class="window-bar">
      <div class="window-dots">
        <span class="dot dot-red"></span>
        <span class="dot dot-yellow"></span>
        <span class="dot dot-green"></span>
      </div>
      <div class="window-title">Links</div>
      <div class="window-meta">Hacker News</div>
    </header>
    <div class="window-body" style="padding: 0;">
      <div class="hn-shell">
        <div class="hn-header" style="padding: 8px 12px; font-size: 0.85rem;">
          <span class="hn-logo" style="padding: 2px 6px; font-size: 0.9rem;">Y</span>
          <span class="hn-title">Hacker News</span>
          <span class="hn-subtitle" style="font-size: 0.75rem;">personal links</span>
        </div>
        <div class="hn-list" style="padding: 12px;">
          <ul class="hn-items" style="padding: 0;">
            {% set linkCount = 0 %}
            {% for post in collections.links %}
              {% if linkCount < 3 %}
                <li class="hn-item" style="grid-template-columns: 30px 1fr; gap: 8px; padding: 8px 0; font-size: 0.8rem;">
                  <div class="hn-rank">{{ loop.index }}.</div>
                  <div class="hn-content">
                    <a class="hn-link" href="{{ post.data.link }}" style="font-size: 0.85rem;">{{ post.data.title }}</a>
                  </div>
                </li>
                {% set linkCount = linkCount + 1 %}
              {% endif %}
            {% endfor %}
          </ul>
          <div style="margin-top: 12px; padding-left: 30px;">
            <a class="window-link" href="{{ '/links/' | url }}" style="font-size: 0.8rem; padding: 6px 10px;">View All →</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
