---
permalink: "/feeds/about.xml"
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>{{ site.title }} - About</title>
    <link>{{ site.url }}/about/</link>
    <description>About posts</description>
    {% for post in collections.about %}
    <item>
      <title>{{ post.data.title }}</title>
      <link>{{ site.url }}{{ post.url | replace("/", "", 1) }}</link>
      <pubDate>{{ post.date.toUTCString() }}</pubDate>
      <guid>{{ site.url }}{{ post.url | replace("/", "", 1) }}</guid>
    </item>
    {% endfor %}
  </channel>
</rss>
